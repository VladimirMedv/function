{
  "compilerOptions": {
    "baseUrl": "app", // Базовая директория
    "paths": {
      "@/*": ["*"] // Алиас для всех файлов в папке app
    }
  },
  "include": ["app/**/*"] // Учитываем все файлы в app
}
